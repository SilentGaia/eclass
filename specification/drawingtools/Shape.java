package drawingtools;

/**
 * Shape is an implementation of the Shape tool used to draw shapes on the lecture slides.
 */
public abstract class Shape  {
    /**
     * X position of the shape.
     */
    int x_pos;
    /**
     * Y position of the shape.
     */
    int y_pos;

    /**
     * Draws the shape at the given x_pos, y_pos.
     */
}
