<html>
<head>
<title>
User Acceptance Testing
</title>
</head>
<body bgcolor=white>
<p align=center>
<font size=+2>
<strong>CSC 309 User Acceptance Testing</strong>
</font>
</p>
<br>
<br>
<h3>
Acceptance Test Plan For File Manager by Tammy Kong
</h3>
<p>
The acceptance test plan for the filemanager of the E-Class.
<blockquote>
<table align=center border=1 cellpadding=4>
  <tr>
    <td valign=top><strong>Case No.</strong>
    <td valign=top><strong>User Inputs and Commands</strong>
    <td valign=top><strong>Expected Output</strong>
    <td valign=top><strong>Remarks</strong>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
    <td valign=top><strong>Results</strong>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
  <tr>
    <td valign=top>1
    <td valign=top>
Start the server by typing java -jar server.jar. 
<br>Then, start the classroom by typing java -jar classroom.jar. 
<br>Login as a student; for example, username is "Tammy Kong" and password is "password".
<br> Click the "files" button at the bottom of the taskbar in the E-Class tool.

    <td valign=top>
User sees the File Manager on top of the main E-Class window. 
    <td valign=top>
Tests if taskbar has made an instance of file manager.
    <td valign=top>
Pass
  <tr>
    <td valign=top>2
    <td valign=top>   Following the previous instructions, click on the "editor" button.
    <td valign=top>   Nothing should come up since user has not created a new presentation.
    <td valign=top>   Tests if editor can edit without a lecture.
    <td valign=top> Pass
 <tr>
    <td valign=top>3
    <td valign=top>   Following the previous instructions, go back to the File Manager.
    <br> Click on the "Create" button.
    <td valign=top>   User should see the Lecture Editor pop up.
    <td valign=top>   Tests the ability to create a lecture.
    <td valign=top> Pass.
	  <tr>
    <td valign=top>4
    <td valign=top>   

        Start the server by typing java -jar server.jar. 
<br>Then, start the classroom by typing java -jar classroom.jar. 
<br>Login as a student; for example, username is "Tammy Kong" and password is "password".
<br> Click the "files" button at the bottom of the taskbar in the E-Class tool.
<br> Click the "import" button ata the center of the file manager.
<br> A file chooser should come up.
<br> Select a html file.
<br> Click the "present" button at the taskbar of the E-Class.
    <td valign=top>   User should see the html file generated on the presentation manager.
    <td valign=top>   Tests the ability to grab an HTML file and generate it into presenation manager.
    <td valign=top> Pass. Only works on Windows Operation System. Unsure of why it does not work on Mac OS.
 <tr>
    <td valign=top>5
    <td valign=top>   Following the previous instructions, click the 'x' button on the top left/right corner.
    <td valign=top>   User should see the file manager close on the 'x' execution.
    <td valign=top>   Tests the ability to close the file manager.
    <td valign=top> Pass
	 
</table>
</blockquote>
</p><p>
</body>
</html>
